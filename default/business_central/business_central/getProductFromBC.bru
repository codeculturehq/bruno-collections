meta {
  name: getProductFromBC
  type: http
  seq: 2
}

get {
  url: {{base_url}}/inway/inwapi/v2.0/companies({{company_id}})/webShopItems
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
